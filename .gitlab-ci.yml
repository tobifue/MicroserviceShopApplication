
stages:
  - build
  - test
  - integrationTest
  - release
  - cleanup

create:
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  script:
    - echo "${CI_JOB_ID}" > CI_JOB_ID.txt
    - ls








release_upload:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  #needs:
    #- job: build
      #artifacts: true
  rules:
    - if: '$RELEASE == "True"'
    - if: '$CI_COMMIT_BRANCH == "master"'
  script:
    - cd deployment
    - ls
  #  - release-cli create --name="My Release" --description="My Release description" --tag-name job-$CI_JOB_ID --ref $CI_COMMIT_SHA
    - |
      release-cli create --name "Release $CI_COMMIT_TAG" --tag-name $CI_COMMIT_TAG \
        --assets-link "{\"name\":\"Executable file\",\"url\":\"https://gitlab.com/some/repo/-/jobs/`cat CI_JOB_ID.txt`CI_JOB_ID.txt\"}"
