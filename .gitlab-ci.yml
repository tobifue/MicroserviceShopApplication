
stages:
  - build
  - test
  - integrationTest
  - release
  - cleanup

create:
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  script:
    - echo "${CI_JOB_ID}" > deployment/CI_JOB_ID.txt
    - echo "TAG=v$(cat VERSION)" >> variables.env 
    - ls
  artifacts:
    paths:
      - deployment



release_upload:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  #needs:
    #- job: build
      #artifacts: true
  rules:
    - if: '$RELEASE == "True"'
    - if: '$CI_COMMIT_BRANCH == "master"'
  script:
#    - cd deployment
    - ls
    - cd deployment
    - ls
    - release-cli create --name="My Release" --description="My Release description" --tag-name job-$CI_JOB_ID 
    #--ref $CI_COMMIT_SHA
  #release:
    #name: "RELEASE"
    #tag_name: $CI_JOB_ID
    #description: 'Release description'

